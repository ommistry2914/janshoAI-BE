import { Request } from "express";

export const AdminService = {
  async findAdmin(req: Request) {
    return {
      // id: user.id,
      // name: user.name,
      // email: user.email,
    };
  },

};
