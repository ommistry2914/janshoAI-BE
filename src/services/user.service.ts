import { Request } from "express";

export const AdminService = {
  async findUser(req: Request) {
    return {
      // id: user.id,
      // name: user.name,
      // email: user.email,
    };
  },

};
